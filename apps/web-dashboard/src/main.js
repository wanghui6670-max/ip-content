// Legacy entry kept temporarily for compatibility.
// The active dashboard entry is `src/boot.js`.
// Keeping this shim avoids breaking older local references while ensuring
// the application runs through the modularized boot flow.

import "./boot.js";

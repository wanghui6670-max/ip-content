{
  "name": "@ip-content/web-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "export:data": "python3 ./scripts/export_dashboard_data.py",
    "dev": "npm run export:data && vite",
    "build": "vite build",
    "build:all": "npm run export:data && vite build",
    "preview": "vite preview",
    "test": "node --test ./test/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.10"
  }
}